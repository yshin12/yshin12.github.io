---
layout: home
author_profile: true
---

# Research Interests:
	- Causal inference and identification problems
	- Applications of statistical decision theory
	- Modern computational algorithm and inference

this is a test launching page. 



<div class="container">
  <div class="container title-image">
    <figure class="figure">
      <img class="figure-img img-fluid rounded" src="images/me.jpg" alt="A picture of me on a mountain">
    </figure>
  </div>
  <section>
    <p>Welcome to my personal website. Here you'll find links to my <a
       href="papers.html">papers</a> and my <a href="cv.pdf">CV</a>. Feel free to contact me at [first initial][middle
       initial][last name][at][popular email service operated by Google].</p>
  </section>
  <hr>
  <section>
    <h2>Publications</h2>
    <ol>
    {% for paper in site.data.papers.papers %}
      <li>{% if paper.link %}<a href="{{ paper.link }}">{{ paper.title }}</a>, {% else %} {{ paper.title }}, {% endif %}
          {% if paper.coauthors %} (with {{ paper.coauthors }}), {% endif %}
          {% if paper.journal %} <span style="font-style: italic">{{ paper.journal }},</span> {% endif %}
          {% if paper.vol_no %} {{ paper.vol_no }}, {% endif %}
          {% if paper.year %} {{ paper.year }}, {% endif %}
          {% if paper.page %} pp. {{ paper.page }} {% endif %}
    {% endfor %}
    </ol>
  </section>
  <hr>
  <section>
    <h2>Working Papers</h2>
    <ol>
    {% for paper in site.data.papers.workingpapers %}
      <li>{% if paper.link %}<a href="{{ paper.link }}">{{ paper.title }}</a>, {% else %} {{ paper.title }}, {% endif %}
          {% if paper.coauthors %} (with {{ paper.coauthors }}), {% endif %}
          {% if paper.journal %} <span style="font-style: italic">{{ paper.journal }},</span> {% endif %}
          {% if paper.vol_no %} {{ paper.vol_no }}, {% endif %}
          {% if paper.year %} {{ paper.year }}, {% endif %}
          {% if paper.page %} pp. {{ paper.page }} {% endif %}
    {% endfor %}
    </ol>
  </section>  
  <hr>
  <section>
    Last Update: August 10, 2022
  </section>
</div>

<div class="container">


</div>
